# assets

Static assets copied verbatim into the Angular bundle (icons, favicons, …).
The default Angular CLI builder publishes every file here into `dist/...`.
Put your files in this directory; nothing is required for the editor to
start.
